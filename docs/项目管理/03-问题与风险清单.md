---
tags: [项目管理, 问题追踪, 风险]
aliases: [问题清单, 风险清单]
maintainer: "/issue-capture Skill"
---

# 问题与风险清单

> [!info] 说明
> 此文档统一登记所有问题、Bug、优化想法、方向性想法和卡点。
> [[02-任务执行追踪表|任务追踪表]] 只引用问题编号，详情在此。新问题由 [[../../.reasonix/skills/issue-capture/SKILL|issue-capture Skill]] 五步法捕获。

---

## 问题统计

| 类型 | 总计 | 新建 | 处理中 | 已解决 | 暂缓 | 放弃 |
|---|---|---|---|---|---|---|
| 🔴 卡点 | 1 | 1 | 0 | 0 | 0 | 0 |
| 🟡 Bug | 0 | 0 | 0 | 0 | 0 | 0 |
| 🟢 优化建议 | 0 | 0 | 0 | 0 | 0 | 0 |
| 🔵 方向想法 | 1 | 1 | 0 | 0 | 0 | 0 |
| ⚪ 其他 | 0 | 0 | 0 | 0 | 0 | 0 |
| **合计** | **2** | **2** | **0** | **0** | **0** | **0** |

---

## 当前问题

### ISS-001：执行链路为 stub，未接入真实连接器 ^iss-001

| 字段 | 内容 |
|---|---|
| 发现周期 | W27（6/30-7/6） |
| 发现人 | 代码审计 |
| 关联任务 | [[02-任务执行追踪表#T06|T06]], [[02-任务执行追踪表#T07|T07]], [[02-任务执行追踪表#T08|T08]], [[02-任务执行追踪表#T09|T09]] |
| 类型 | 🔴 卡点 |
| 严重程度 | 高（阻塞 Excel 导入和数据接入链路） |
| 状态 | 🆕 新建 |

**描述**
当前接入任务执行 API 通过 RQ 队列调用的 worker 任务是 `run_echo_sync`——一个仅打印任务名称的占位函数。真实的 SqlServerConnector、文件连接器、数据写入逻辑均未接入。

**根因**：不是代码没写好——SqlServerConnector 代码已完成——而是 **ERP 数据库的连接信息和访问权限还没有从客户/运维侧拿到**（IP、端口、数据库名、只读账号、网络白名单），导致无法在真实环境中验证和串联。

**影响**
- T06 虽然 API 完成度 85%，但核心执行链路不通（Worker 中跑的是 echo 占位）
- T07 的 SqlServerConnector 代码已完成，但缺少真实数据库连接信息无法验证
- T08、T09 完全被阻塞
- 数据浏览页只能读取已有测试数据，看不到新导入数据

> [!warning] 本质是外部依赖卡点，非技术阻塞。代码侧可并行推进 T04/T05/T10 等不依赖 ERP 数据的任务。

**建议方案**
1. **代码侧**：将 `run_echo_sync` 替换为真实的 `run_ingestion_task` 函数（1 人天）
2. **协调侧**：获取 ERP SQL Server 的 IP、端口、数据库名、只读账号、网络白名单（需客户/运维配合）
3. 拿到连接信息后，优先接入 SqlServerConnector 验证，再逐步接入文件连接器
4. 在 RQ Worker 中以同步方式运行连接器，FastAPI 层仅做任务调度

**建议优先级**：P1 本周处理

**前置依赖**：ERP 数据库连接信息（IP/端口/数据库名/只读账号/网络白名单），需客户或运维提供

**预估工时**：1-2 人天

**解决记录**
- 解决周期：—
- 解决方案：—
- 实际工时：—

---

## 方向想法

### ISS-002：阶段 2 语义建模应采用场景驱动而非全量铺开 ^iss-002

| 字段 | 内容 |
|---|---|
| 发现周期 | W28（7.7-7.11） |
| 发现人 | 项目复盘 |
| 关联任务 | T15, T16, T18, T19 |
| 类型 | 🔵 方向想法 |
| 严重程度 | 低（阶段 2 尚未启动） |
| 状态 | 🆕 新建 |

**描述**
当前 `docs/本体建模/` 下的建模成果覆盖 20 个业务对象、41 条语义关系、36 条业务规则，属于通而全的纸上建模，覆盖整个制造域但缺少针对具体场景的深度。直接按这个全量模型进入代码开发会导致周期长、验证慢。

**建议方案**
阶段 2 启动时，先选定一个高价值场景（如订单交付风险分析、库存异常预警），针对该场景做深度建模——仅建模该场景涉及的对象/属性/关系/规则，代码实现后快速验证，跑通再扩到下一个场景。

**建议优先级**：P3 阶段 2 启动时处理

**前置依赖**：阶段 1 完成，产品侧确认试点场景

**预估工时**：方向性建议，不涉及当前开发

---

## 已解决

> 暂无

---

## 暂缓 / 放弃

> 暂无
